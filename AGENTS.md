# Kai 復縁タロット専用ルール

## 適用範囲

以下のルールは、このリポジトリで **Kai 復縁タロットの投稿画像を新規作成・修正・検品するときだけ** 適用する。

- 車、転職、その他アカウントの画像には適用しない。
- 投稿本文だけを作る作業には適用しない。
- 一般的な画像生成依頼には適用しない。
- 適用範囲を自己判断で拡大しない。

画像作業を始める前に、必ず `docs/KAI_TAROT_IMAGE_RULES.md` を全文確認する。
同ファイルを、この対象における画像仕様の正本とする。

## 絶対条件

- 過去のタロットカード画像・カードPNG・素材集は削除対象であり、新規投稿へ一切流用しない。
- 過去素材の背景変更、切り抜き、再配置、再合成、色変更も禁止。
- 共通カード素材データを保持・参照・再作成しない。カードを含む情景は毎週すべて新規制作する。
- 3択1本は、親1枚・返信3枚の独立した計4ファイル。
- 2択1本は、表向きのカード2枚を載せた親画像1枚だけで完結させる。
- 4分割、一覧、コラージュは禁止。
- すべての画像を1080×1350に統一しない。
- 3択親画像は1080×608。
- 3択親画像は、投稿テーマに合わせて毎回新規生成した完成画像を使う。カードPNGの貼り付けやPython合成は禁止。
- 3択親画像のカードは3枚を同じ大きさで横並びにし、過去画像の座標値を新規制作の素材テンプレートとして再利用しない。
- 3択のA・B・Cは各カードの真上かつカード外側に置く。
- 3択返信画像は1080×1350。カードを含む情景全体を各返信ごとに新規制作する。
- 2択と通常投稿の寸法は、既存実装または確定済み仕様を確認し、推測で決めない。
- 3択親画像は、表向きのカード3枚と `A・B・C` だけ。
- 2択は本文にA・Bの選択肢を明記し、対応する表向きカード2枚を画像に載せる。
- 2択画像は1080×1350。各カード430×645、上端240px、間隔40px、A・Bの中心Y座標970px。
- 2択にA返信・B返信、返信結果、返信画像は作らない。
- 2択を「伏せた2枚から選び、返信で結果を見る形式」へ変更しない。
- 返信画像は、対応する結果カード1枚が自然に存在する情景。文字なし。
- 通常投稿は、内容とカードが自然に結びつく場合だけ画像を付ける。
- 通常投稿の画像は、タロットカードが自然に存在する情景。文字なし。
- 背景を生成してからカードや文字を後付け合成しない。
- `card_ids`、共通カード一覧、カードPNG、画像生成Pythonを制作元にしない。
- GitHubは完成画像の保存・投稿だけを行う。画像が欠けている場合は自動生成せず停止する。
- 親画像と各返信画像は、そのまま別々に投稿できる完成ファイルにする。
