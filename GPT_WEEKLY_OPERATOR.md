# Kai 復縁タロット｜週次運用指示書

この指示書は、制作専用チャットで週1回使う。目的は原稿を量産することではなく、前週の結果を使って翌週の投稿精度を上げること。

## アカウント

- 名前: kai 復縁タロット
- 対象: 復縁、音信不通、連絡するか迷う人
- 価値: 不安を煽らず、占いを入口に次の行動まで分かりやすく伝える
- 投稿時刻: 毎日07:00、12:00、20:00（日本時間）
- 3択占い: 週次企画で確定した本数（固定本数を過去ルールから流用しない）

## 毎週の作業順

1. `reports/latest/analysis.md`、`data/post_history.csv`、前週の予約内容を読む。
2. 表示数、返信率、再投稿・引用・シェア、形式、テーマ、時間帯を比較する。
3. 成功要因・失敗要因を複数仮説で記録する。断定できないことは可能性と書く。
4. 翌週の日付、月初・月末、季節、祝日、恋愛上の強いイベントを確認する。
5. 候補を30件以上作り、需要・共感・保存・返信・重複・継続性で評価する。
6. 21件を採用し、本文・画像内文章・3択結果を一組として制作する。
7. 過去のカード画像・カードPNG・素材集は一切使わず、カードを含む情景を投稿内容に合わせて毎週新規制作する。
8. 文章と画像をこのチャットで修正し、完成後に`data/weekly_package.json`と
   `generated/`を含むGitHub投入用ZIPを出力する。
9. 変更する改善項目は最大3つに絞り、実験を1件定義する。

## 21投稿の条件

- 7日×3投稿。`morning`、`noon`、`evening`を毎日1件ずつ。
- `three_choice`の本数と配置は、その週に承認された企画を正とする。
- 使用可能形式:
  - `three_choice`
  - `relationship_tip`
  - `question`
  - `poll`
  - `checklist`
  - `message_example`
  - `psychology`
  - `empathy`
  - `short_advice`
  - `event_countdown`
  - `event_today`
  - `story`
- 週6形式以上を使う。
- 3択以外へタロットカードを無理に付けない。
- 3択は表向きの異なる3枚を見せ、「直感で選ぶ」と案内する。
- 3択結果はA・B・Cの返信として、本音・状況・次の行動をそれぞれ書く。
- 強いイベント当日は必ず扱う。重要日が近い場合は自然なカウントダウンも使う。

## AIっぽさを消す規則

- 同じ書き出し、段落数、語尾、CTAを連続させない。
- すべてを長文にしない。短文、問い、会話例、チェックリスト、体験描写を混ぜる。
- 「今日のカードは」「〜かもしれません」を連発しない。
- 抽象的な励ましだけで終えない。具体的な場面、判断基準、例文を入れる。
- 相手の気持ちや復縁成功を断定しない。不安を煽って課金へ誘導しない。
- タイトル、本文の完全重複は禁止。酷似した原稿も禁止。

## 画像

Kai復縁タロットの画像制作・修正・検品では、`docs/KAI_TAROT_IMAGE_RULES.md`を正本とする。以下の旧記述と矛盾する場合は、同ファイルを優先する。このルールを車・転職・その他の画像制作へ適用しない。

- 画像の制作と修正はGPT側で完了させる。GitHub上のPythonに画像を作らせない。
- `image.kind: none`なら本文だけで投稿する。それ以外は完成画像を必ず同梱する。
- 実用画像は`text_card`、`checklist`、`comparison`、`message_example`から選ぶ。
- 画像内の文章は短くし、本文の全文を詰め込まない。
- タイトルと補助見出しで同じ意味を繰り返さない。装飾目的の言い換えは入れない。
- 3択だけ`tarot_choice`を使う。
- 3択を含むすべての画像で、過去のカード素材を流用・加工・再合成しない。カードを含む情景全体を毎週新規制作する。
- 3択画像をカードID、共通カードPNG、Python合成から生成しない。
- GitHub側は完成済み画像を投稿するだけとし、欠損時は自動生成せず停止する。
- 3択の選択画像1枚と、A・B・Cの結果画像3枚を完成させる。
- 画像と本文でカード名、本音、状況、次の行動を一致させる。
- 無意味な一枚カード、カード名だけの画像、大きすぎる余白は禁止。

## 週次出力

`data/weekly_package.json`だけを完成形として出力する。最上位には以下を含める。

- `schema_version`
- `week_start`
- `generated_at`
- `analysis.summary`
- `analysis.success_patterns`
- `analysis.improvements`（最大3件）
- `experiment`
- `posts`（21件）

投稿ごとの必須項目:

- `key`
- `date`
- `slot`
- `status: ready`
- `format`
- `topic`
- `title`
- `body`
- `cta_type`
- `image`
- `image_path`（画像投稿の場合）

3択のみ追加:

- `replies`（A、B、C）
- 各返信の`image_path`

完成後は「21件、各日3件、承認済みの3択本数、週6形式以上、重複なし、
指定された完成画像がすべて存在する」を自己確認する。

## 上限・失敗時

- ChatGPT上の手動運用ではOpenAI APIを使わない。
- 将来自動API化しても、週1回、最大8モデル呼び出し、最大20検索、再試行1回までを上限にする。
- 入力不足、形式不正、重複、日付不整合があれば投稿データを更新せず停止する。
- 原稿または画像が欠けていれば停止する。GitHub側で穴埋め生成しない。
- エラー時に穴埋め用のランダム原稿を投稿しない。
